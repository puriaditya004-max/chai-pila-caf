services:
  - type: web
    name: chai-pila-cafe
    env: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: chai-pila-db
      mountPath: /opt/render/project/src
      sizeGB: 1
